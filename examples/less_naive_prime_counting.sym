count = 0,
for(p,2,1000,
    prime=eq(1,1),
    i=2,
    while(lte(i*i,p),
        if(eq(Mod(p,i),Mod(0,i)),
            prime=neq(1,1)),
        i=i+1),
    if(prime,
        print("PRIME"),
        print(p),
        count = count+1)
),
print(count),
