check_prime(p) {
    prime=true
    i=2
    while (lte(i*i,p)) {
        if (eq(Mod(p,i),Mod(0,i))){
            prime=false
        },
        i=i+1
    }
    prime
}
count = 0
for (p,2,100000) {
    if (check_prime(p)) {
        count = count+1
    }
}
println(count)
