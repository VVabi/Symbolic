count = 0,
for(p,2,1000,
    prime=true,
    i=2,
    while(lte(i*i,p),
        if(eq(Mod(p,i),Mod(0,i))){
            prime=false
        },
        i=i+1
    ),
    if(prime){
        count = count+1
    }
),
println(count),
